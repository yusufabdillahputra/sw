$(document).ready(function () {
    'use strict'

    var component_location = 'app/transaksi/penjualan';

    /**
     * Konfigurasi window untuk pencarian barang
     */
    $('#e_wsearch').window({
        closed: true,
        collapsible: false,
        minimizable: false
    });

});